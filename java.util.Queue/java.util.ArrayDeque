* resizable array, keine Größenbeschränkung
* nicht thread-safe
* keine null-elemente
* This class is likely to be faster than Stack when used as a stack, and faster than LinkedList when used as a queue.
* im Schnitt alles in O(1)
* fail-fast iterator

Wann nehmen?
* wenn man Deque braucht
* anstatt LinkedList als queue oder Stack als stack
